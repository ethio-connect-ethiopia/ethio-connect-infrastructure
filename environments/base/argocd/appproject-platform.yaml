apiVersion: argoproj.io/v1alpha1
kind: AppProject
metadata:
  name: platform
  namespace: argocd
spec:
  description: Shared platform core infrastructure across clusters and environments.
  sourceRepos:
    - https://github.com/PrimeWale/ethio-connect.git
  destinations:
    - server: '*'
      namespace: ethio-connect-*
  clusterResourceWhitelist:
    - group: '*'
      kind: '*'
  namespaceResourceWhitelist:
    - group: '*'
      kind: '*'
